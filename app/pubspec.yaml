name: kap
description: "KAP — gamified fundamental analysis: judge anonymized company cards (MVP: Dagens Runde)."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  dio: ^5.9.2
  flutter:
    sdk: flutter
  flutter_card_swiper: ^7.2.0
  freezed_annotation: ^3.0.0
  json_annotation: ^4.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  freezed: ^3.0.0
  json_serializable: ^6.14.0

flutter:
  uses-material-design: true
